<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aim.mall.basic.idgen.mapper.AimIdGenRuleMapper">

    <!-- 基础字段列表 -->
    <sql id="Base_Column_List">
        id, prefix, date_pattern, current_max_seq, step_size, create_time, update_time
    </sql>

    <!-- 根据前缀和日期格式查询 -->
    <select id="selectByPrefixAndPattern" resultType="com.aim.mall.basic.idgen.domain.entity.AimIdGenRuleDO">
        SELECT
        <include refid="Base_Column_List"/>
        FROM aim_idgen_rule
        WHERE prefix = #{prefix}
        AND date_pattern = #{datePattern}
    </select>

    <!-- 分配号段（原子更新） -->
    <update id="allocateSegment">
        UPDATE aim_idgen_rule
        SET current_max_seq = current_max_seq + #{stepSize},
            update_time = NOW()
        WHERE prefix = #{prefix}
        AND date_pattern = #{datePattern}
    </update>

</mapper>
